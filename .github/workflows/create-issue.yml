name: Create Issue with Current Time in Markdown

on:
  schedule:
    # disable the cron job for testing
    - cron: '0 0 0 0 0' # Runs every hour at 44 minutes

jobs:
  create-issue:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v3
